# generated by configure / remove this line to disable regeneration
prefix="/cross-tools"
exec_prefix="${prefix}"
bindir="${exec_prefix}/bin"
libdir="/home/dl/ramdisk/clfs_cross_tools/system/mips64-64/sources/step_12_isl-0.14-build/.libs"
datarootdir="${prefix}/share"
datadir="${datarootdir}"
sysconfdir="${prefix}/etc"
includedir="/home/dl/ramdisk/clfs_cross_tools/system/mips64-64/sources/step_12_isl-0.14-build/../isl-0.14/include"
package="isl"
suffix=""

for option; do case "$option" in --list-all|--name) echo  "isl"
;; --help) pkg-config --help ; echo Buildscript Of "isl Library"
;; --modversion|--version) echo "0.14"
;; --requires) echo : ""
;; --libs) echo -L${libdir} "-L/cross-tools/lib" "-lisl -lgmp"
       :
;; --cflags) echo -I${includedir} "-I/cross-tools/include"
       :
;; --variable=*) eval echo '$'`echo $option | sed -e 's/.*=//'`
;; --uninstalled) exit 0 
;; *) ;; esac done
